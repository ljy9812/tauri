# Tasks: p2-ohos-accessibility

## 1. 插件 crate 骨架

- [x] 1.1 创建 `plugins-workspace/plugins/accessibility/`:Cargo.toml(links/metadata.platforms.support:其他平台 none、ohos partial;`[target.'cfg(target_env = "ohos")'.dependencies]` 加 `openharmony-ability-plugin-accessibility = { path = "../../../openharmony-ability/crates/plugin-accessibility" }` + `futures-executor = "0.3"`;serde/thiserror/tauri workspace 依赖)、build.rs(COMMANDS 三命令)、error.rs(Unsupported/Accessibility 变体)、tsconfig.json、rollup.config.js、package.json(@tauri-apps/plugin-accessibility)、guest-js/index.ts(getFontScale/isScreenReaderEnabled/isTouchExploreEnabled/onAccessibilityStateChange;listen 封装)
- [x] 1.2 src/lib.rs:Builder 双分支(OHOS invoke_handler 指 ohos.rs 命令,非 OHOS 指 commands.rs stub)+ setup:OHOS 分支 register_plugin(AccessibilityBridgePlugin)(失败 log::error)+ spawn 线程 block_on(client.subscribe_state_change(handler → app_handle.emit("accessibility-state-changed", enabled)))
- [x] 1.3 src/ohos.rs:三命令 async 实现(块作用域 drop MutexGuard 后 .await;错误映射 Error::Accessibility);src/commands.rs:非 OHOS stub 返回 Error::Unsupported

## 2. 权限与构建

- [x] 2.1 permissions/default.toml(allow-get-font-scale / allow-is-screen-reader-enabled / allow-is-touch-explore-enabled);跑 build 触发 tauri_plugin::Builder 生成 autogenerated
- [x] 2.2 guest-js `npm run build` 产 dist-js,校验 index.js/index.cjs/index.d.ts 非 0 字节

## 3. examples/api 接入

- [x] 3.1 src-tauri/Cargo.toml OHOS target 依赖 + src/lib.rs OHOS 块 `.plugin(tauri_plugin_accessibility::init())` + capabilities/ohos-plugins.json 加 `"accessibility:default"`
- [x] 3.2 plugins.ts:auto 用例 getFontScale(断言正 number)+ 查询命令不 panic 用例;manual 用例 onAccessibilityStateChange(开关屏幕阅读器,console 指引)

## 4. spec 修订与验证

- [x] 4.1 修订 ohos-platform-limitations R230:最小 API 边界声明(查询/事件可用、ExtensionAbility 不可用、ARIA 归 ArkWeb)+ 汇总表行更新
- [x] 4.2 cargo check:插件 crate host + aarch64-unknown-linux-ohos 双侧 0 error;examples/api src-tauri OHOS target check 通过
- [x] 4.3 真机验证(run-tests.sh desktop):auto 用例绿;manual 用例按指引实测 fontScale 数值、screen-reader 查询权限行为、状态事件;记录实测结论回填 spec Open Questions
