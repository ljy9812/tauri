# Tasks: p2-ohos-screenshot

## 1. 插件 crate 骨架

- [x] 1.1 创建 `plugins-workspace/plugins/screenshot/`：Cargo.toml（links="tauri-plugin-screenshot"、platforms.support 逐平台声明参照 accessibility Cargo.toml、`[target.'cfg(target_env = "ohos")'.dependencies]` 加 `openharmony-ability-plugin-screenshot = { path = "../../../openharmony-ability/crates/plugin-screenshot" }`、tauri/serde/thiserror/log workspace 依赖）、build.rs（COMMANDS = ["capture_webview", "pick_color"] snake_case）、error.rs（Unsupported/Screenshot 变体 + from 映射）、tsconfig.json、rollup.config.js、package.json（@tauri-apps/plugin-screenshot）、guest-js/index.ts（captureWebview/pickColorAt）
- [x] 1.2 src/lib.rs：crate 级 `#![cfg(not(any(target_os = "android", target_os = "ios")))]` + Builder 双分支（OHOS invoke_handler 指 ohos.rs，非 OHOS 指 commands.rs stub）+ OHOS setup 仅 log（无 register_plugin、无事件订阅、无 futures-executor 依赖）
- [x] 1.3 src/ohos.rs：`client()` helper（APP.lock().map_err 处理 mutex 中毒，guard 不跨 .await，同 accessibility 模式）+ `capture_webview<R>(webview: tauri::Webview<R>)` / `pick_color<R>(webview, x, y)`——`webview.label()` 作 bridge id；返回插件自有 DTO `CapturedImageDto`/`RgbaDto`（bridge 层类型未 derive Serialize，须插件侧定义 + `#[serde(rename_all = "camelCase")]` + From 转换）；src/commands.rs：非 OHOS stub 返回 Error::Unsupported

## 2. 权限与构建

- [x] 2.1 permissions/default.toml（allow-capture-webview / allow-pick-color）；cargo build 触发 tauri_plugin::Builder 生成 autogenerated 权限
- [x] 2.2 guest-js `npm run build` 产 dist-js，校验 index.js/index.cjs/index.d.ts 非 0 字节

## 3. examples/api 接入

- [x] 3.1 src-tauri/Cargo.toml OHOS target 依赖 + src/lib.rs OHOS 块 `.plugin(tauri_plugin_screenshot::init())` + capabilities/ohos-plugins.json 加 `"screenshot:default"` + package.json `file:` 依赖 + pnpm install
- [x] 3.2 新建 `src/views/Screenshot.svelte`（5 已知纯色色块 + 截图预览 + 取色结果展示），注册进 App.svelte views 数组
- [x] 3.3 新建 `src/lib/tests/ohos-screenshot.ts`（auto captureWebview 断言 / side-effect pickColorAt 红块阈值断言 / manual 截图预览）注册进 test-runner

## 4. spec 修订与验证

- [x] 4.1 修订 ohos-platform-limitations R229：应用内 webview 截图取色最小 API 边界声明（插件已提供；系统级 @ohos.screenshot 仍不可用）+ 汇总表行更新
- [x] 4.2 cargo check：插件 crate host + aarch64-unknown-linux-ohos 双侧 0 error；examples/api src-tauri OHOS target check 通过
- [x] 4.3 真机验证（run-tests.sh desktop）：auto/side-effect 用例绿；红色块取色实测 RGB 值回填 design.md 阈值；记录实测结论
