# ohos-monitor-real-values Tasks

- [x] 1. openharmony-ability app.rs：import + `refresh_rate()`/`display_width()`/`display_height()` 方法
- [x] 2. tao `video_modes()` refresh_rate 取 `app.refresh_rate()`
- [x] 3. tao `size()` 取 DisplayManager 物理像素 + content_rect 回退
- [x] 4. tao `monitor_from_point`（EventLoopWindowTarget + Window）边界判定
- [ ] 5. 构建验证 `default_display_width/height` 函数名与返回类型
