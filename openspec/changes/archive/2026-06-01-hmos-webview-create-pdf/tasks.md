# Implementation Tasks: HarmonyOS WebView createPdf

## Phase 1: ArkTS 层实现

- [x] **1.1 更新 JsHelper 接口**
  - 文件: `native_ability/src/main/ets/webview/Utils.ets`
  - ~~新增 `createPdf: (path: string, config: Record<string, number | boolean>, callback: (success: boolean) => void) => void`~~
  - 新增 `createPdf: (path: string, callback: (success: boolean) => void) => void`

- [x] **1.2 实现 ProxyJsHelper.createPdf**
  - 文件: `native_ability/src/main/ets/webview/Utils.ets`
  - 支持窗口未就绪时缓存操作
  - 窗口就绪后重放 pendingOperations

- [x] **1.3 实现 buildJsHelper 中的 createPdf**
  - 文件: `native_ability/src/main/ets/webview/DefaultWebview.ets`
  - 导入 fileIo: `import { fileIo } from '@kit.CoreFileKit'`
  - 定义 DEFAULT_PDF_CONFIG 常量
  - ~~实现配置合并逻辑~~ (已移除：直接使用 DEFAULT_PDF_CONFIG)
  - 调用 controller.createPdf()
  - 处理 PDF 数据写入文件
  - 错误处理和文件句柄释放

- [x] **1.4 同步 package 目录**
  - 文件: `package/src/main/ets/webview/Utils.ets`
  - 文件: `package/src/main/ets/webview/DefaultWebview.ets`
  - 注: package 目录被 .gitignore 忽略，构建时自动生成

## Phase 2: Rust openharmony-ability 层实现

- ~~[x] **2.1 定义 PdfConfig 结构体**~~ (已移除)
  - ~~文件: `crates/ability/src/helper/webview.rs`~~
  - ~~7 个 Option 字段: width, height, margin_top/bottom/left/right, should_print_background~~
  - ~~实现 Default trait~~
  - ~~实现 `to_napi_map()` 方法，序列化为 HashMap，仅包含 Some 字段~~
  - ~~key 使用 camelCase 匹配 ArkTS 命名~~

- [x] **2.2 实现 Webview::create_pdf()**
  - 文件: `crates/ability/src/helper/webview.rs`
  - ~~通过 NAPI ObjectRef 获取 "createPdf" 函数~~
  - 通过 NAPI ObjectRef 获取 "createPdf" 函数 (签名: path + callback，无 config)
  - 创建 NAPI closure 作为回调
  - 调用 ArkTS 侧 createPdf 函数
  - ~~传递 path、config_map、callback~~
  - 传递 path、callback

## Phase 3: wry 层实现

- [x] **3.1 实现 InnerWebView::create_pdf()**
  - 文件: `wry/src/ohos/mod.rs`
  - ~~Re-export PdfConfig: `pub use openharmony_ability::PdfConfig`~~ (已移除)
  - 委托给 openharmony_ability::Webview::create_pdf()

- [x] **3.2 实现 wry::WebView::create_pdf()**
  - 文件: `wry/src/lib.rs`
  - 使用 `#[cfg(target_env = "ohos")]` 区分平台
  - ~~OHOS 版本接受 `Option<ohos::PdfConfig>`~~
  - ~~其他平台接受 `Option<()>`~~
  - OHOS 版本和其他平台统一签名: path + callback (无 config)

- [x] **3.3 添加平台 stub**
  - 文件: `wry/src/webkitgtk/mod.rs`
  - 文件: `wry/src/webview2/mod.rs`
  - 文件: `wry/src/wkwebview/mod.rs`
  - 文件: `wry/src/android/mod.rs`
  - 所有 stub 返回 `Ok(())`，不调用 callback
  - ~~签名: `_config: Option<()>`~~ → 签名: path + callback (无 config)
  - 注: 已知问题 #1，当前不影响 OHOS 平台

## Phase 4: Tauri Runtime 层实现

- [x] **4.1 添加 WebviewDispatch trait 方法**
  - 文件: `tauri/crates/tauri-runtime/src/lib.rs`
  - `fn create_pdf(&self, path: String, callback: Box<dyn Fn(bool) + Send + 'static>) -> Result<()>`

- [x] **4.2 实现 tauri-runtime-wry**
  - 文件: `tauri/crates/tauri-runtime-wry/src/lib.rs`
  - 添加 `CreatePdf(String, Box<dyn Fn(bool) + Send + 'static>)` 到 WebviewMessage 枚举
  - 实现 WryWebviewDispatcher::create_pdf()
  - 在事件循环中添加 dispatch 处理

- [x] **4.3 实现 mock runtime**
  - 文件: `tauri/crates/tauri/src/test/mock_runtime.rs`
  - 返回 `Ok(())` 并调用 `callback(true)`

## Phase 5: Tauri Public API 层实现

- [x] **5.1 实现 Webview::create_pdf()**
  - 文件: `tauri/crates/tauri/src/webview/mod.rs`
  - 接受 `impl AsRef<std::path::Path>` 作为 path 参数
  - 接受 `impl Fn(bool) + Send + 'static` 作为 callback
  - 委托给 dispatcher.create_pdf()

- [x] **5.2 实现 WebviewWindow::create_pdf()**
  - 文件: `tauri/crates/tauri/src/webview/webview_window.rs`
  - 直接委托给内部的 Webview
  - 不添加 `#[cfg(desktop)]` 限制

## Phase 6: 测试应用实现

- [x] **6.1 创建 test_create_pdf 命令**
  - 文件: `examples/api/src-tauri/src/cmd.rs`
  - 获取 main webview window
  - 调用 create_pdf() 使用沙箱路径
  - emit "create-pdf-result" 事件

- [x] **6.2 注册命令**
  - 文件: `examples/api/src-tauri/src/lib.rs`
  - 添加到 generate_handler! 宏

- [x] **6.3 配置 ACL 权限**
  - 文件: `examples/api/src-tauri/capabilities/run-app.json`
  - 添加 "allow-test-create-pdf"

- [x] **6.4 创建权限文件**
  - 文件: `examples/api/src-tauri/permissions/autogenerated/test_create_pdf.toml`
  - allow/deny 配对

## Phase 7: 前端测试

- [x] **7.1 添加自动测试**
  - 文件: `examples/api/src/lib/tests/core.ts`
  - 测试名称: "webview.createPdf (default A4)"
  - 验证完整链路: invoke → Rust → ArkTS → 文件写入 → callback → emit → resolve
  - 断言: result 以 "true:" 开头，包含 ".pdf"

- [x] **7.2 添加手动测试**
  - 文件: `examples/api/src/views/TestRunner.svelte`
  - 按钮: "Create PDF & Verify (check file on device)"
  - 使用 await listen 避免竞态条件
  - 显示结果和 hdc 命令供手动验证

## Phase 8: 构建和部署

- [x] **8.1 本地构建**
  - 使用 ohos-build skill
  - 编译 Rust 代码
  - 打包 HAR
  - 构建 HAP

- [x] **8.2 设备部署**
  - 安装 HAP 到 OHOS 设备
  - 运行自动测试
  - 验证 PDF 文件生成

- [x] **8.3 代码推送**
  - 备份到 C:\code\createPdf-backup
  - 推送到 sb-fy-sb fork 的 feature/hmos-create-pdf 分支
  - 创建 PR 到 Eulogizethesun/ohdev

## 完成状态

**总任务数:** 23  
**已完成:** 23  
**未完成:** 0  
**完成率:** 100%

## ~~已知问题~~ (已修复)

以下问题已记录在 design.md 的"检视发现"章节中：

1. ~~**⚠️ 中** - 平台 stub 的 callback 泄漏（非 OHOS 平台）~~ → **已修复**：stub 已删除，公共 API 加 `#[cfg(target_env = "ohos")]` 门控
2. ~~**⚠️ 中** - NAPI 层异常路径的 callback 泄漏~~ → **已修复**：常见错误路径（env/函数获取失败）在 callback 被 move 前调用 `callback(false)`；灾难性 NAPI 失败后的 callback 无法回收为已知限制
3. **💡 低** - 窗口不存在时静默返回

这些问题当前不影响 OHOS 平台功能，已记录供未来修复。
