## 1. 插件 crate 骨架

- [x] 1.1 新建 `plugins-workspace/plugins/huawei-account/Cargo.toml`:`name = "tauri-plugin-huawei-account"`,`links = "tauri-plugin-huawei-account"`,`[build-dependencies] tauri-plugin = { workspace = true, features = ["build"] }`,`[dependencies] tauri/serde/serde_json/thiserror = { workspace = true }`;`[target.'cfg(target_env = "ohos")'.dependencies] openharmony-ability = { path = "../../../openharmony-ability/crates/ability", features = ["account"] }`;`[package.metadata.platforms.support]` ohos=partial、windows/linux/macos=none
- [x] 1.2 新建 `build.rs`(`tauri_plugin::Builder::new(COMMANDS).build()`,COMMANDS = login/silent_login/logout)
- [x] 1.3 ~~把插件加入 workspace members~~ → `plugins-workspace` 用 `members = ["plugins/*", ...]` glob,新建 `plugins/huawei-account` 自动包含,无需手动加

## 2. Rust 侧 — 数据模型与错误

- [x] 2.1 新建 `src/models.rs`:`AccountInfo`(`#[serde(rename_all="camelCase")]`,字段 uid/open_id/union_id/display_name/avatar_uri/authorization_code:String + access_token:Option<String>),与 p1 结构一致;`#[cfg(target_env="ohos")] impl From<openharmony_ability::AccountInfo>`
- [x] 2.2 新建 `src/error.rs`:`enum Error { Unsupported, Cancelled, NotLoggedIn, Other(String) }`+`Serialize`(字符串)+`from_napi_reason`(剥 "rejected: "/"Error: " 前缀取 code 映射)+ 4 个 UT

## 3. Rust 侧 — 命令(OHOS 真实 + desktop stub)

- [x] 3.1 新建 `src/ohos.rs`(`cfg(target_env = "ohos")`):`#[tauri::command] async fn login/silent_login/logout`,各 `HuaweiAccount::new().xxx().await.map_err(|e| Error::from_napi_reason(&e.reason))`,login/silent_login 经 `AccountInfo::from` 转换
- [x] 3.2 新建 `src/commands.rs`(`cfg(not(target_env = "ohos"))`):三个同名 stub `Err(Error::Unsupported)`
- [x] 3.3 新建 `src/lib.rs`:`pub use {Error, AccountInfo}`;`#[cfg(not(ohos))] mod commands` + `#[cfg(ohos)] mod ohos` 门控(修复初版漏 gate 导致 `__cmd__*` 重复);`Builder`(`#[derive(Default)]`)+ 双 `build()` cfg + `init()`

## 4. JS 侧

- [x] 4.1 新建 `guest-js/index.ts`:`AccountInfo` interface + `login()`/`silentLogin()`/`logout()` 调 `invoke('plugin:huawei-account|login'|'silent_login'|'logout')`(命令名用 Rust snake_case)
- [x] 4.2 新建 `package.json`/`rollup.config.js`(用 `../../shared/rollup.config.js`)/`tsconfig.json`(extends `../../tsconfig.base.json`),仿 os 插件

## 5. 权限

- [x] 5.1 新建 `permissions/default.toml`(含 `allow-login`/`allow-silent-login`/`allow-logout`)。**注**:`allow-*.toml` 由 `build.rs` 自动生成到 `autogenerated/commands/{login,silent_login,logout}.toml`(非手写,修正 design D7);`schemas/schema.json` + `reference.md` 亦自动生成

## 6. examples/api 集成

- [x] 6.1 `examples/api/src-tauri/Cargo.toml` 加 `tauri-plugin-huawei-account = { path = "../../../../plugins-workspace/plugins/huawei-account" }`(正常 dep)
- [x] 6.2 `examples/api/src-tauri/src/lib.rs` 加 `.plugin(tauri_plugin_huawei_account::init())`

## 7. 构建与验证

- [x] 7.1 `cargo check -p tauri-plugin-huawei-account --target aarch64-unknown-linux-ohos` 通过(经 tauri workspace 构建;account feature 经插件统一启用)
- [x] 7.2 `cargo check -p tauri-plugin-huawei-account`(host/desktop)通过,desktop stub 路径编译,不引入 openharmony-ability
- [x] 7.3 `cargo check -p api --target aarch64-unknown-linux-ohos` 通过(插件注册进 examples/api)
- [x] 7.4 examples/api 构建启动,插件加载,命令可 invoke(ohos 真实登录留 Phase 3)
  - **注(2026-07-30 verify)**:经 ohos-build run-tests.sh 构建 examples/api(含 huawei-account 插件)→ hvigor `BUILD SUCCESSFUL`(**p1 `account.ets` 首次经真实消费者编译通过**)→ 签名安装启动成功 → 自动测试 242/244(同基线无回归)。Windows desktop runtime invoke(stub 返回 unsupported)未单独跑——stub 简单且 cargo check + error UT 覆盖,低风险,留待 Phase 3/4 一并验证。

## 补充验证

- [x] error 分类 UT 4/4 通过(host `cargo test`):parse_not_logged_in / parse_unsupported / parse_other_preserves_code_msg / parse_bare_reason
